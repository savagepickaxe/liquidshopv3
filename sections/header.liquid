{% render 'header-menu' %}

{% schema %}

{
  "name": "Header",
  "settings": [
    {
      "type": "header",
      "content": "Header"
    },
    {
      "type": "text",
      "id": "header_text",
      "label": "Header Text",
      "default": "Welcome to our store"
    },
    {
      "type": "text",
      "id": "header_subtext",
      "label": "Header Subtext",
      "default": "We are glad to see you here"
    }
  ],
  "presets": [
    {
      "name": "Header",
      "category": "Header"
    }
  ]
}

{% endschema %}